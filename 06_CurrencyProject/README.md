most hooks use and return most thing in js 
good practice is which ever need jsx write jsx if need js write js
hook name add use in first

we get data in string format and need to covert in json format so we can handle